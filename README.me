# Implementation Using Scikit-Learn  
- import `KNeighborsClassifier` from `sklearn` for KNN classification.
- split the dataset into train and test sets using `train_test_split`.
- import various evaluation metrics from `sklearn.metrics`, including `accuracy_score`.

 
